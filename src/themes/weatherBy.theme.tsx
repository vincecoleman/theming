import { createTheme } from "../styles/styles";

const weatherByTheme = createTheme({
  colors: {
    primary: "red",
    secondary: "teal",
    warning:"maroon"
  },
});

export default weatherByTheme;
